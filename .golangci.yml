version: "2"
linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - gofmt
    - goimports
linters-settings:
  gofmt:
    simplify: true
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
