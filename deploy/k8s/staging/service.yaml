apiVersion: v1
kind: Service
metadata:
  name: funnelbarn
spec:
  selector:
    app.kubernetes.io/name: funnelbarn
    app.kubernetes.io/environment: staging
  ports:
    - name: http
      port: 8080
      targetPort: http
